outputFilter:
  dropEvents:
    module: drop-events
    debug: false
    filters:
      severity:
        # include: !!js/regexp error|warning
        exclude: !!js/regexp /debug/i
      service:
        exclude: !!js/regexp /snmpd/i